import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Ionic-app-side-menu-navigation',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
